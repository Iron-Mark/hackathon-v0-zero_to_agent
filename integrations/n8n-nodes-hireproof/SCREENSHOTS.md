# n8n Screenshot Checklist

Capture these before community-node submission:

- Node search result showing `HireProof`.
- `HireProof API` credential screen with production base URL and masked API key.
- `Run audit` operation with `text`, `location`, and `mode`.
- Successful `Run audit` execution output showing `verdict`, `riskScore`, `summary`, `redFlags`, and `evidence`.
- `Run async audit` operation with `webhookUrl`.
- Immediate `202 processing` response.
- Callback receiver output containing the completed `AuditReport`.

Do not claim n8n marketplace approval until the community listing is accepted.
