# Make Screenshot Checklist

Capture these before Make review submission:

- Custom App overview screen showing HireProof metadata.
- API-key connection setup using the `x-api-key` header.
- `Audit job post` module configuration.
- Successful sync audit output with `id`, `verdict`, `riskScore`, `confidence`, `summary`, `redFlags`, `greenFlags`, `evidence`, and `nextSteps`.
- `Audit job post async` module configuration and `202 processing` response.
- `Get API health` module result.

Do not claim Make marketplace approval until review is accepted.
