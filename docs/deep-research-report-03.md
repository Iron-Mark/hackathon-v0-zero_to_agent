# Archived Research Memo

This document is preserved as research context. It contains older citations and speculative comparison notes; use `docs/hireproof-action-plan.md`, `docs/remaining-work.md`, and live verification output as the implementation source of truth.

# Executive Summary
HireProof is largely functional but still missing some high-impact polish and integration to dominate the community vote.  The Next.js app, backed by a model-check-protocol (MCP) server and streaming audit endpoints, builds the core v0 experience【9†L47-L56】【56†L38-L45】.  Both unit tests and production builds pass【45†L8-L12】, and the **production site is deployed at `hireproof-sigma.vercel.app`**【56†L6-L10】.  The README, docs site, and SKILLS guide show the four evidence-gathering tools are implemented【9†L47-L56】【18†L18-L27】.  However, the **ChatSDK Slack bot and Vercel Workflow (WDK) features are still “credential-gated” stubs**: the endpoints exist but require secrets to run.  For example, `/api/chat/hireproof` returns Slack credential status【82†L5-L13】, and `/api/workflows/audit` returns a “credential-required” notice unless a `WORKFLOW_SECRET` is set【73†L71-L79】.  These half-baked features leave gaps that savvy judges will notice.

In contrast, leading showcase projects like **HackPilot** (multi-model coding agent) and **Nexus Sales** (AI sales co-pilot) present polished, working demos with clear value propositions【61†L139-L144】【3†L279-L287】.  For instance, Nexus Sales requires no signup and immediately generates outreach messages from a pasted URL【3†L279-L287】, and the eSIM Agent flows through a full checkout【61†L108-L112】.  Compared feature-by-feature, HireProof matches or exceeds top entries in core functionality (e.g. full audit pipeline, live evidence links, responsive UI【56†L45-L53】), but lags in finishing touches: mobile refinement, clear demo flows, export/share features, multimedia assets, and explicit track coverage in docs.

**Key findings:** HireProof’s MVP is solid but incomplete on peripheral points.  The MCP tools are in place【9†L47-L56】 and documentation is thorough, but mobile UI responsiveness needs testing and the live demo must be rock-solid.  Missing or gated features (Slack/WDK, extension, login flow) should either be finished or clearly labeled as “coming” in the submission.  Compared to top projects, HireProof needs more visual polish and evidence of judge-focused docs and media.  We compile a prioritized action plan below with quick wins (UI fixes, copy updates), medium tasks (video/screenshots, community outreach), and longer-term work (ChatSDK and Workflow enablement).  Finally, we list a concrete vote-day checklist and rule compliance audit to ensure no surprises.

## 1. Repo Audit: Features & Track Implementation
- **Core MVP (v0 + MCP):** The codebase implements a Next.js **v0 app** for pasting a job post, running it through an agent, and showing a verdict.  The **MCP skill tools are fully defined**: `search_company`, `news_check`, `jobs_compare`, and `local_presence` are documented in SKILLS.md【9†L10-L19】【18†L18-L27】 and available via the `/api/mcp` endpoint.  The audit pipeline `/api/audit` streams investigation events, and `/api/v1/audit` returns JSON, as confirmed by the deployment summary【56†L26-L34】【56†L38-L45】.  Demo mode (with seeded data when API keys are missing) is built in【56†L45-L53】 and local storage saves history.  Tests (`auth-core.test.mjs`, `runtime-wiring.test.mjs`) pass and the production build succeeds【45†L8-L12】.  The UI is responsive and uses Tailwind with custom theming【56†L45-L53】.  **Verdict:** v0/MCP track is implemented end-to-end.

- **ChatSDK (Slack/Discord) Track:**  The repo includes the ChatSDK packages (`@chat-adapter/slack`, `chat` library) and a `/api/chat/hireproof` endpoint.  A GET on that endpoint reports Slack credential status (via `getSlackCredentialStatus()`【82†L5-L13】) and explains usage; the POST handler calls `createChatReply()` to produce a verdict.  In other words, the Chat SDK logic is wired up in code, but **no valid tokens are provided**.  The `/api/chat/hireproof` GET response explicitly shows `"platformWebhook": "/api/webhooks/slack"` and uses `getSlackCredentialStatus()`【82†L5-L13】 – in practice this will return false without environment vars.  The `/api/webhooks/slack` route (implied by `/api/webhooks/slack`) is not documented here, but the Chat plan notes it exists.  **Verdict:** Slack integration exists in code but is not active in production (credentials required).  This should be called out in docs; if left unaddressed, judges may mark Chat SDK as unproven.

- **WDK (Workflow) Track:**  The `/api/workflows/audit` endpoint is implemented.  Its GET returns a JSON with `credentialStatus` and expected usage (method=POST, required headers)【73†L10-L18】.  The POST route either starts the Vercel Workflow (if `WORKFLOW_SECRET` is set) or returns a “credential-required” notice【73†L71-L79】.  In practice, our summary confirms it’s credential-gated: “Workflow route is implemented with WDK startAuditWorkflow plumbing. Set WORKFLOW_SECRET to claim live durable execution.”【73†L71-L79】.  **Verdict:** WDK is stubbed out but not active without secrets.  This track is acknowledged in docs and code, but no live workflow runs will happen without a secret.  The implementation exists (calls `startAuditWorkflow`), so we should mention it under “WDK track supported (pending setup)”.

- **Stability and Completeness:**  Build/test logs are clean【45†L8-L12】.  All core pages (/audit, /history, /developer, /docs) return 200 in local smoke tests【45†L107-L115】.  The deployment summary lists ~30 static routes including `/audit`, `/history`, `/trends`, and dynamic `/api/audit`【56†L26-L34】.  Missing pieces include: final homepage design polish, hooking up a real badge/verification, enabling Chrome extension (folder `/extension` exists).  The strategy docs (in `docs/` and archived reports) indicate remaining work on BYOK keys, badge DNS, and exports【45†L75-L84】【45†L92-L97】.

**Summary:** The repo **covers the core submission requirements**: v0 app with AI, evidence tools, scoring, history and docs.  Minor issues: some pages in development (developer portal behind login, extension folder unused), and ChatSDK/WDK features not truly live. All such gaps must be clearly documented or fixed.

## 2. Demo Site Testing (hireproof-sigma.vercel.app)
The production app is at **https://hireproof-sigma.vercel.app**【56†L6-L10】. (Alternative “iron-marks-projects” Vercel URL also exists.)  According to the deployment summary, primary pages and endpoints are live【56†L26-L34】【56†L38-L45】.  We attempted to access them, but the environment may restrict direct crawling. However, based on [56], here is what to check:

- **Uptime/Reliability:** The Vercel dashboard shows recent successful builds and a ready deployment【56†L65-L71】. Smoke-test endpoints listed in the Next-Step plan【45†L109-L117】 should be tried (if accessible) to confirm 200 responses:
  - Home (`/`), Audit form (`/audit`), Trends (`/trends`), History (`/history`), Docs (`/docs`).
  - API: `/api/health`, `/api/chat/hireproof`, `/api/workflows/audit`.
  Each should return JSON or 200. (If secrets are missing, `/api/chat/hireproof` and `/api/workflows/audit` should indicate “credential required” rather than 500.) [45] shows exactly these calls【45†L109-L117】.

- **Performance:** The summary claims “build: succeeded” and no errors【56†L12-L19】. Real performance testing (load times, network logs) isn’t possible here, but ensure pages hydrate quickly, especially audit streaming. Any long API calls (SERPAPI) should be asynchronous with spinners.

- **Mobile UX:** The site uses mobile-first Tailwind and checks “Full responsive design”【56†L45-L53】. Still, manually verify key pages on a phone viewport: input form readability, buttons, and result tables should scale. The “Demo Examples” buttons (if present) must be tappable. Visual polish on small screens is crucial since judges may vote from phones.

- **Demo flows:** Walk through the **Audit flow**: Paste a sample job text and run it. Check that each step appears (company lookup, news scan, salary, maps). The summary asserts *evidence links and receipts* are shown【56†L26-L34】. Verify:
  1. Text input form accepts URL and text.
  2. Initiating “Investigate” triggers the SSE stream in `/api/audit`, showing progress.
  3. The final report page (`/audit/{id}`) should display a verdict, score, and clickable evidence URLs (per SKILL format【36†L194-L203】). Confirm the examples in SKILL.md produce output.
 If SerpAPI or AI keys are absent, it should fall back to demo fixtures. The summary says “Demo mode working (no API keys required)”【56†L45-L53】, so test with keys blank in localStorage. The “History” page should log past investigations in localStorage. Check that saved reports have working shareable links.
 Any form errors (like empty input) should be handled gracefully (e.g. “Please enter text”); if not, that’s a quick fix.
 2. **Export/Evidence:** The plan and code suggest PDF/JSON export of reports (via html2canvas/jspdf). Test the “Print” or “Download” buttons. If PDF export is present, ensure it includes all findings. If “export JSON” is available, verify it downloads properly.
 3. **Login/Signup:** It appears account creation is **optional** (developer portal uses it, but normal use does not require login). Confirm that no popup or forced login interrupts the audit flow. (If “Pricing” or “Settings” pages do require login, ensure they’re clearly marked as developer-only).

- **Flaky or missing:** Without keys, the SERPAPI calls will return limited data. The `/api/health` endpoint should honestly report any down status. Check `/api/health`; it should include “storage”, “model”, “live search” status (phase 4 acceptance)【45†L109-L117】. If any part fails (e.g. rate-limit on demo key), fix messaging.  Also test the “Demo Example” quick buttons: do they auto-fill samples and run? Ensure those links work. If any required file (like manifest in `/extension`) is broken/missing, remove it or mark as future work.

**Summary:** The deployed app appears functional per logs【56†L26-L34】. Key priorities: verify actual flows on a device, fix any 404 or UI bugs, and ensure the judge sees a stable audit demonstration.

## 3. Competitive Feature Comparison

We compare HireProof against five leading submissions to identify gaps.  These projects were chosen for similar complexity or domain: **HackPilot** (coding agent framework), **Nexus Sales** (AI sales co-pilot), **Scout** (due-diligence agent), **eSIM Agent** (full-stack purchase demo), and **Rejection Reframer** (job application analysis).  Below is a summary of their value props, and a comparative table on key criteria.

- **HackPilot:** Multi-model decision agent for coding, with architecture diagram【61†L139-L142】. Focuses on showing system design rather than user demo flows.
- **Nexus Sales:** AI assistant for agencies. Takes a company URL/name and outputs scored lead and personalized outreach messages, including follow-ups【3†L279-L287】. Emphasizes “no auto-sending, human in control” on a Next.js site.
- **Scout (Due Diligence):**  User inputs a company, and it instantly generates an investment “deal memo” with market, competition, risks, etc【2†L96-L102】. Demonstrates a polished v0 UI with stock data/LLM.
- **eSIM Agent:**  End-to-end travel eSIM purchase flow with payment.  Simple UI walkthrough from selection to checkout【61†L108-L112】.
- **Rejection Reframer:**  Parses a rejection email and original application, pinpoints mistakes, rewrites the application pitch, and gives actionable advice. Emphasizes data-driven feedback (no clichés)【3†L333-L341】.

These projects tend to have: clear one-sentence value (“AI copilot for X”); live demos requiring minimal input; full feature flows (search → result); polished UI; and often video/screenshots on their showcase.  By comparison, **HireProof** offers a niche but important function (“Verify suspicious job posts”). It has a strong evidence-backed process (scorecard with citations), but we need to highlight it as clearly as others.

| **Project**           | **Demo Reliability** (demo works out-of-box) | **Value Clarity** (snappy one-liner) | **Visual Polish** | **Mobile Friendly** | **Multimodal Inputs** (text, URL, images) | **Evidence / Export** (structured output) | **Track Coverage** (v0, ChatSDK, WDK) | **Docs & Architecture** (judge-ready) | **Campaign Assets** (video/screenshots) |
|-----------------------|---------------------------------------------|--------------------------------------|-------------------|---------------------|------------------------------------------|-----------------------------------------|--------------------------------------|---------------------------------------|---------------------------------------|
| **HireProof**         | Core audit flow works (SSE stream and JSON APIs)【56†L26-L34】, demo mode seeded✅. Some parts (Chat, Workflow) show status messages but require keys.   | Clear problem/solution: “Is this job offer a scam?” but needs bullet summary. (Can improve tagline.) | Clean Tailwind UI; currently corporate/simple styling. Lacks final design touches or hero image. | Claimed “responsive”【56†L45-L53】; must verify forms and report layout on mobile screens.  | Accepts text or URL; image attachments (screenshots) partially handled (OCR on images?)—less obvious. Possibly missing true image parsing.  | Outputs verdict with links and score【36†L194-L203】. Should add PDF/CSV export buttons if not present (html2canvas/jspdf are included)【39†L38-L41】. | Implements **v0+MCP** fully【9†L47-L56】; ChatSDK Slack implemented but gated【82†L5-L13】; WDK stub present【73†L71-L79】. Need to flag in docs which tracks are live. | Extensive docs site (`/docs` has many pages) and in-repo docs【12†L13-L21】. Lacks a short system diagram (could borrow from SKILL.md) but SKILL.md is thorough. | No video in showcase; would benefit from a 1–2 min demo video. Few screenshots (except code), needs polished landing image. Social links missing. |
| **HackPilot**【61†L139-L144】  | Demo is essentially a static diagram. No interactive site; just architectural explanation and screenshot. (Relies on concept rather than live test.) | One-liner present (“Multi-Model Agent Framework”)【61†L139-L142】. Abstract, may confuse general users. | Presentation is more schematic than UI-focused. Not highly visual (blueprint diagram). | Not applicable (no real UI).  | Textual/diagram only. No user-uploaded input. | No export; delivers concept. | Focuses on v0 / multi-LLM design; no ChatSDK/WDK since it’s just a concept. | Good diagram in screenshot【61†L139-L144】 and detailed doc. Judges will read architecture. | No video; fewer community assets likely. |
| **Nexus Sales**【3†L279-L287】| Live demo on landing; no signup needed; presumably works live. Uses multiple AI tools (Claude, web scrape). | Very clear: “Paste a site, agent researches and drafts outreach”【3†L279-L287】. | Likely a polished UI (screenshots not on community page, but likely a dashboard/landing). | Claimed mobile support (Next 15, but need check). Probably responsive by default. | Text/URL only (pastes company/lead). No image upload. | Generates emails & follow-ups. May support CSV export of leads (not stated). | Covers v0 (Next.js, AI Gateway)【3†L279-L287】. No ChatSDK mentioned, no WDK. | Some docs on site (maybe blog). Figure shows “Built on Next.js 15, AI Gateway…” to satisfy judges. | Likely has demo video/screenshots (social presence implied by “built on” details). |
| **Scout (Due Diligence)**【2†L94-L102】| Fully interactive “type and run” interface. The UI is attractive and fast (e.g. site looks clean). Lives demo is presumably reliable. | Clear and compelling: “Generate a complete deal memo for any company in seconds”【2†L96-L102】. | Modern UI (likely charts/tabs for market overview, risks). Professional look in screenshot. | Likely fully responsive (info-heavy but built for investors). | Input text (company name), maybe ticker symbol. Possibly URL. No mention of image. | Outputs structured sections (market overview, risks, metrics). Probably allows copy/paste or PDF. | Uses v0; possibly integrated multiple data sources. (No mention of Chat/WDK). | The showcase description is concise. May have supplementary readme on GitHub. | Possibly video link (financial demos often have walkthrough), and screenshots. |
| **eSIM Agent**【61†L108-L112】    | Website flows end-to-end with payment (Stripe demo perhaps). Very user-friendly demo with immediate action. | Simple: “Buy a travel eSIM via an agent with payment”【61†L108-L112】. | Very polished: uses real-world images/icons (implied by travel theme). Looks like a product site. | Likely highly mobile optimized (travelers on phones). The description praises responsive design. | Text input (destination info?), maybe select from lists. No images. | Payment flow is the “export” of purchase. Not an LLM output, but result is the eSIM purchase. | Pure v0 (Next + Stripe API). No AI tracks beyond maybe answering queries about plans. | Likely minimal docs (site explains features), not heavy on AI technical detail. | Probably has nice screenshots of UI; maybe a demo video (shopping UX). |
| **Rejection Reframer**【3†L333-L341】 | Interactive text tool. Presumably reliable output (no mention of bugs). Likely demo works on sample rejection. | Very clear, niche value: “Reads your rejection to pinpoint what went wrong and rebuilds your pitch”【3†L333-L341】. | The showcase text is lengthy and sells the feature, but UI detail is unknown. Possibly a text area and card output. | Probably mobile-friendly (text UI easily responsive). | Text input only (paste email + job ad). No images. | Outputs rewritten application and analysis. Should include copy/paste or download. | v0 (mentions Gemini), no ChatSDK or WDK. | Detailed description given; may have code link or repo. | Possibly screenshots of the “receipt” UI, but no video noted. |

**Insights from comparison:** HireProof’s core audit flow, evidence-backed report, and focus on scam detection are strong differentiators.  Unlike general copilot agents, it operates in a high-stakes domain (job fraud).  Its value proposition is **clear and urgent** (“Don’t get scammed”), but could be stated more crisply on the landing page (similar to how Scout/Rejection frame the outcome).  Visually, HireProof is more utilitarian; adding some graphics or a clear hero image could help it stand out.  Evidence linking is a unique strength – no other project shows raw search results with URLs as proof.  We must exploit that: a quick comparison chart or diagram (similar to HackPilot’s architecture) would impress judges.

In terms of *“judge-proof”* materials, HireProof’s detailed SKILL spec【34†L9-L17】【36†L192-L201】 and strategy docs give it an edge: we should present a summary diagram (like HackPilot’s) that maps the investigation pipeline.  Also, none of the competitors appear to offer Slack/Discord integrations yet, so if we can at least demo the **ChatSDK skill via a simple POST request or Slack channel mention**, that would differentiate HireProof.  Finally, community assets (video/screenshots) are lacking in our submission. We should immediately produce a short screen-recorded demo (≤2 min) highlighting the audit process, verdict, and links.  That is **lower effort, high impact** content.

## 4. Action Plan: Prioritized Tasks

**🔥 Quick Wins (≤24h, *Immediate Impact*):** Fix low-hanging fruit to ensure a flawless demo launch.
- **Final UI polish & responsiveness:** Run a mobile audit of the site. Tweak any layout issues (e.g. button size, text wrapping) on small screens. Ensure the header/title clearly states the problem (“Verify job posts”). A crisp hero section can replace any placeholder content. *Effort: 2–4h, Risk: low, Vote Impact: medium-high.*
- **Bug fixes in flows:** Test each audit path. If any error cases (empty input, slow API) crash, patch them. Make sure the “Demo example” buttons auto-fill and run. Confirm “History” entries link correctly. *Effort: 1–2h, Risk: low, Vote Impact: medium.*
- **Credential gating clarifications:** In the docs/FAQ section of the demo, **explicitly note** that Slack/Workflow are optional extras requiring setup. (E.g.: “Slack bot coming soon – ping @HireProofBot on Slack to try it (requires admin approval).”) This avoids judge confusion. *Effort: 1h, Risk: low, Impact: medium (avoids penalty).*
- **Finalize README/submission copy:** The “Submit” form will show title and description. Ensure the one-liner and description are tight. Example: “**HireProof:** AI agent that checks if a job offer is a scam by gathering evidence (news, company presence, salary norms). Returns Safe/Caution/High-Risk with proof【36†L194-L203】.” Show track tags: “v0 + MCPs (with Chat/Workflow optional)”. *Effort: 1h, Risk: low, Impact: high (first impressions matter).*

**🏃 Medium Effort (24–72h, *Boost Impact*):** Tasks that require more work but pay off in vote count or polish.
- **Demo video + screenshots:** Record a 90–120s video walking through a sample job post investigation (capture both web UI and part of the SSE stream or final report). Also take clear screenshots for the submission form and social. Host the video on YouTube or Loom and embed/link it in the submission. *Effort: 4h, Risk: low, Impact: high (visual media draw votes).*
- **Social/community outreach plan:** Draft concise social posts (Twitter, LinkedIn, Discord) announcing the demo. Engage relevant groups (AI dev, job seeker communities). Prepare a friendly pitch: “@Connection👀 check out this AI agent that spotted a fake job post!” Make sure the project name and link are easy to share. *Effort: 4h, Risk: low, Impact: high (votes are social-driven).*
- **Finish Chrome Extension (if time):** The `/extension` folder suggests a browser extension demo. If not already live, either deploy it or hide that menu to avoid confusion. If deployable quickly, a packaged extension that captures page text and sends to HireProof would be very compelling. *Effort: ~6h, Risk: medium (testing), Impact: medium (bonus point).*
- **Add export/share features:** If not done, enable PDF/JSON export of the audit report (using html2canvas/jspdf already installed【39†L37-L40】). E.g., “Download Report” button that saves the verdict. This signals completeness. *Effort: 4–8h, Risk: low-medium, Impact: medium.*

**🚀 Long-Term (>72h, *Future Building*):** These strengthen the project but may not complete in time; plan to mention progress for judges.
- **ChatSDK multi-platform (Discord):** After proving Slack works locally, set up a quick Discord bot using the same code. Host it on a free Heroku or Glitch if needed. Even a basic Discord “!hireproof” command would wow judges. *Effort: 1–2 days, Risk: medium, Impact: high (unique track coverage).*
- **WDK full integration:** If possible, deploy with a dummy WORKFLOW_SECRET and demonstrate one background workflow call (with callback). Show a log or result to prove durability. *Effort: 2+ days, Risk: high (complex), Impact: high (fulfills another track).*
- **Refine Developer Portal:** The `/developer` section is robust but unlabeled. If time, update branding and maybe open a few features (like getting one saved API key) to show off security design. *Effort: 1+ days, Risk: medium, Impact: low (mostly internal).*

Each task above is **straightforward**: just do it.  No need for “five options” or theories – just code it and test it. Don’t get lost in minor design debates; the goal is a *shippable, judge-pleasing demo*.

## 5. Vote-Day & Judge Checklist

**Vote-Day Checklist (Community Votes):**
- **Announce it:** At voting start (May 4, 7:00AM PT), immediately post on Twitter/LinkedIn/Discord with the project link and ask friends/colleagues to vote. (“Hey folks, HireProof is live on @vercel hackathon showcase – please support by voting!”).
- **Social blitz:** Use personal and relevant group accounts. Do **not** spam; a single friendly reminder is fine. Provide instructions (“Vote for HireProof in Cursor Hackathon showcase”).
- **Monitor & pivot:** If votes seem low midday, perhaps share a new angle (e.g. a specific beneficiary like “This just saved me from a scam” story).
- **Supporters rally:** Encourage at least 20-30 people in your network to cast their one vote. (One per person rule.)
- **Stay available:** Be online to answer any community questions or bugs reported. If someone says “it broke for me”, fix it ASAP and comment back “Done.”
- **Celebrate milestones:** If you hit, say, 50 votes, post an update thanking supporters (keeps momentum).
- **No cheat:** Don’t create fake accounts or bribe votes – that risks disqualification and is against rules.

**Judge-Proof Pack Checklist:**
- **One-page summary:** Prepare a PDF (or markdown doc) summarizing the app: tracks, features, tech stack. Include the main architecture diagram (from SKILL.md or draw a flow chart of the audit pipeline). Judges love a quick overview slide.
- **Usage logs:** Save a screenshot or log snippet showing a complete audit run (with evidence URLs) to prove it works as advertised.
- **Website visit:** Provide the exact production URL and confirm it’s deployed (e.g. “Live demo: https://hireproof-sigma.vercel.app (checked April 29)”).
- **Video link:** Add the demo video (with timestamp instructions) to the submission if possible.
- **Documentation link:** Include a link to the documentation site (`/docs`) and GitHub repo. Point out the SKILLS page and strategy docs to show depth.
- **Mobile screenshot:** One responsive screenshot (e.g. audit on mobile view) to show mobile readiness.
- **Compliance notes:** Document how sign-up is optional and credentials-secured features are handled. (E.g. “Chat Bot and Workflow require setup tokens; core demo needs no login.”)
- **Politeness:** The submission form has text fields for “What did you build?” and “Why is it cool?” Ensure those are honest, no hype. Cite specific outputs.

## 6. Rule Compliance & Risks

- **Public Access:** The hackathon rules require a **live site on Vercel** that anyone can try (with sign-in if optional). HireProof meets this: the audit page is public. The developer portal is behind login, but that’s for API keys only (acceptable). Ensure **no part of the main demo requires an account**. If “history” or “reports” require login, mention that only full pipeline save requires login. Otherwise, mark “login not required” in rules notes.
- **Single Submission / Fair Voting:** We have one submission. Do *not* use multiple accounts to vote or ask for votes in violation of rules. The plan emphasizes organic outreach.
- **No Hidden Triggers:** Avoid gating any core flow behind external sign-ups. For example, if the Slack bot requires joining a workspace, label it as optional. Make sure the app works without any hidden payment or login wall. (The “Pricing” page likely advertises credits; ensure it’s not mandatory to click.)
- **Credit Providers:** If using third-party services (SERPAPI, Groq), it’s fine per rules, but mention “powered by SERPAPI/Groq” to be safe.
- **Content Licensing:** All code is open-source. The SKILL.md cites public facts. Our content is original or cited. No copy of others’ assets.
- **Legal Compliance:** No illegal or disallowed content. HireProof is security-focused (anti-scam); that’s totally on-theme and positive.

Any gated feature (Slack/Webhooks, WDK, Chrome extension) must be clearly documented as optional/prototype. Judges shouldn’t be confused by a missing Slack token. The `/api/chat/hireproof` and `/api/workflows/audit` endpoints already **explicitly state their credential status** in JSON【82†L5-L13】【73†L71-L79】, which is good – just highlight that in docs.

**Conclusion:**  Execute this plan with urgency. Fix the little things now, prepare your showcase assets, and treat vote day like a mission. No fluff: add missing features or mark them “coming soon,” polish the UI, then **promote** the hell out of it on May 4. The code and concept are solid【56†L26-L34】【9†L47-L56】; it just needs final push to beat the competition. Good luck, *Execution Beast*.
